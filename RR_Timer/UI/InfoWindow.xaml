<Window x:Class="Race_timer.UI.InfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="InfoWindow" Height="100" Width="{x:Static SystemParameters.PrimaryScreenWidth}" 
        WindowStyle="None" ShowInTaskbar="False" Topmost="True" Left="0" Top="0"
        Style="{StaticResource MaterialDesignWindow}"
        Icon="/UI/onlyRmini.png" IsTabStop="False" 
        ResizeMode="NoResize">
    <Grid>
        <ScrollViewer x:Name="InfoScrollViewer" Margin="10 0" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled" CanContentScroll="False">
            <TextBlock x:Name="InfoText" Text="Info" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="{DynamicResource InfoFontSize}"/>
        </ScrollViewer>
    </Grid>
</Window>